# Allow audioserver to r/w to f0_value sysfs
allow audioserver sysfs_f0_value:file rw_file_perms;

# Allow audioserver to r/w to ultrasound_device
allow audioserver ultrasound_device:chr_file rw_file_perms;
